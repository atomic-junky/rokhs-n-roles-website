---
import { PortableText } from "@portabletext/react";
import { imageType } from "../lib/sanityPortableText.jsx";

const { value } = Astro.props;

const components = {
  types: {
	image: imageType
  },
};
---

<PortableText value={value} components={components}/>
